# Environment Configuration
# Copy this file to .env and fill in values
---
# FRONTEND (Next.js)
# URL of backend API (can be relative if using nginx proxy, or absolute URL)
NEXT_PUBLIC_API_URL=http://localhost:8000

# BACKEND (FastAPI - Python)
# Security secret for JWT tokens - CRITICAL: Change this in production
SECRET_KEY=<your-secret-key-change-in-production>

# CORS Configuration for Backend
# Comma-separated list of allowed origins
# Local development: Include localhost ports
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,http://127.0.0.1:3000
# Production (ECS with ALB): Leave empty for same-origin security
# ALLOWED_ORIGINS=

# AI Integration (Optional)
# Get key here: https://aistudio.google.com/apikey
GEMINI_API_KEY=<your-gemini-api-key>

# Image Generation (Optional)
# Get key here: https://unsplash.com/developers
UNSPLASH_ACCESS_KEY=<your-unsplash-access-key>

# DATABASE (PostgreSQL)
# Database Credentials
POSTGRES_USER=<your-postgres-user>
POSTGRES_PASSWORD=<your-postgres-password>
POSTGRES_DB=<your-postgres-db>

# Docker internal connection string
# Note: Ensure these match the credentials above
DATABASE_URL=<your-database-url>
